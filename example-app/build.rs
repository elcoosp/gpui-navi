fn main() {
    // // Generate route tree from src/routes/
    // let config = navi_codegen::NaviConfig::from_file("navi.config.json")
    //     .expect("Failed to read navi.config.json");
    // navi_codegen::generator::write_route_tree(&config).expect("Failed to generate route tree");
}
