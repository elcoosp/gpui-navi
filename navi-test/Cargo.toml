[package]
name = "navi-test"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
gpui = { workspace = true }
navi-router = { path = "../navi-router" }
futures = "0.3"
tokio = { version = "1", features = ["rt", "macros"] }
