[package]
name = "navi-test"
version.workspace = true
edition = "2024"

[dev-dependencies]
gpui = { workspace = true }
navi-router = { path = "../navi-router" }
futures = "0.3"
tokio = { version = "1", features = ["rt", "macros"] }
